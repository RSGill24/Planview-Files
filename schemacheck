SELECT TABLE_SCHEMA, TABLE_NAME 
FROM INFORMATION_SCHEMA.VIEWS
WHERE TABLE_NAME LIKE '%initiatives%'
   OR TABLE_NAME LIKE '%epics%'
   OR TABLE_NAME LIKE '%tasks%'
ORDER BY TABLE_SCHEMA, TABLE_NAME;
